using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class GetInput : MonoBehaviour {
    /*
    public string tag_this;

	void Start () {
	}
	
	void saveString() {
        Text input_text = gameObject.GetComponent<Text>();
        PlayerPrefs.SetString(input_text.text, input_text.text);
    }

    void saveInt()
    {
        Text input_text = gameObject.GetComponent<Text>();
        PlayerPrefs.SetInt(tag_this, int.Parse(input_text.text));
    }

    void saveFloat()
    {
        Text input_text = gameObject.GetComponent<Text>();
        PlayerPrefs.SetFloat(tag_this, float.Parse(input_text.text));
    }
    */
}
